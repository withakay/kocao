## 1. Implementation
- [x] 1.1 Create the baseline project layout for API, operator, harness, and web packages.
- [x] 1.2 Add make targets and scripts for bootstrap, test, lint, local run, and `kind` cluster lifecycle.
- [x] 1.3 Introduce shared configuration loading with environment validation.
- [x] 1.4 Add Kubernetes deployment skeletons and a `dev-kind` local overlay.
- [x] 1.5 Add local image flow for OrbStack (`docker build` + `kind load docker-image`) and local pull policy defaults.
- [x] 1.6 Verify bootstrap, `kind` bring-up, and local image deployment commands execute in a fresh checkout.
