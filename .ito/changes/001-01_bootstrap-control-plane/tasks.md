## 1. Implementation
- [~] 1.1 Create the baseline project layout for API, operator, harness, and web packages.
- [ ] 1.2 Add make targets and scripts for bootstrap, test, lint, local run, and `kind` cluster lifecycle.
- [ ] 1.3 Introduce shared configuration loading with environment validation.
- [ ] 1.4 Add Kubernetes deployment skeletons and a `dev-kind` local overlay.
- [ ] 1.5 Add local image flow for OrbStack (`docker build` + `kind load docker-image`) and local pull policy defaults.
- [ ] 1.6 Verify bootstrap, `kind` bring-up, and local image deployment commands execute in a fresh checkout.
