apiVersion: v1
kind: Service
metadata:
  name: control-plane-api
spec:
  selector:
    app: control-plane-api
  ports:
    - name: edge
      port: 80
      targetPort: edge
      nodePort: 30080
  type: NodePort
