# Reference the existing ghcr-pull secret (copied from arc-runners namespace).
# This resource declaration ensures kustomize knows about it for the namespace.
apiVersion: v1
kind: Secret
metadata:
  name: ghcr-pull
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: PLACEHOLDER
