{
  "runtimes": {
    "node": {
      "versions": ["25.6.1", "24.13.1", "22.22.0"],
      "check": "node --version"
    },
    "bun": {
      "versions": ["1.3.9"],
      "check": "bun --version"
    }
  },
  "tools": {
    "bash": "bash --version",
    "claude": "claude --version",
    "codex": "codex --version",
    "curl": "curl --version",
    "fd": "fd --version",
    "gh": "gh --version",
    "git": "git --version",
    "jq": "jq --version",
    "make": "make --version",
    "mise": "mise --version",
    "nvim": "nvim --version",
    "opencode": "opencode --version",
    "pi": "pi --version",
    "rg": "rg --version",
    "sandbox-agent": "sandbox-agent --version",
    "tmux": "tmux -V",
    "yq": "yq --version"
  }
}
